{% extends "layouts/page.njk" %}

{% block content %}
  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">

      <a href="{{ backLink }}" class="govuk-back-link" data-testid="back-link">{{ t('common.back') }}</a>

      {% if error %}
        <div class="govuk-error-summary" data-module="govuk-error-summary" role="alert" data-testid="error-summary">
          <h2 class="govuk-error-summary__title">{{ t('common.errorSummaryTitle') }}</h2>
          <div class="govuk-error-summary__body">
            <ul class="govuk-list govuk-error-summary__list">
              <li><a href="#recycling-operation-code">{{ error }}</a></li>
            </ul>
          </div>
        </div>
      {% endif %}

      <h1 class="govuk-heading-l" data-testid="page-heading">{{ heading }}</h1>

      <form method="post">
        <input type="hidden" name="crumb" value="{{ crumb }}">

        <div class="govuk-form-group{% if error %} govuk-form-group--error{% endif %}">
          <fieldset class="govuk-fieldset" {% if error %}aria-describedby="recycling-operation-code-error"{% endif %}>
            <legend class="govuk-fieldset__legend govuk-fieldset__legend--m">
              {{ label }}
            </legend>
            {% if error %}
              <p class="govuk-error-message" id="recycling-operation-code-error"><span class="govuk-visually-hidden">Error:</span> {{ error }}</p>
            {% endif %}
            <div class="govuk-radios govuk-radios--inline" data-module="govuk-radios" id="recycling-operation-code">
              {% for option in options %}
                <div class="govuk-radios__item">
                  <input class="govuk-radios__input"
                         id="recycling-operation-code-{{ option.value | lower }}"
                         name="recyclingOperationCode"
                         type="radio"
                         value="{{ option.value }}"
                         {% if option.checked %}checked{% endif %}
                         data-testid="option-{{ option.value }}">
                  <label class="govuk-label govuk-radios__label" for="recycling-operation-code-{{ option.value | lower }}">
                    {{ option.text }}
                  </label>
                </div>
              {% endfor %}
            </div>
          </fieldset>
        </div>

        <button type="submit" class="govuk-button" data-module="govuk-button"
                data-testid="continue-button">
          {{ continueButton }}
        </button>
      </form>

      <a href="{{ cancelUrl }}" class="govuk-link" data-testid="cancel-link">{{ cancelLink }}</a>

    </div>
  </div>
{% endblock %}
