{% extends "layouts/page.njk" %}

{% block content %}
  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">

      <a href="{{ backLink }}" class="govuk-back-link" data-testid="back-link">{{ t('common.back') }}</a>

      {% if error %}
        <div class="govuk-error-summary" data-module="govuk-error-summary" role="alert" data-testid="error-summary">
          <h2 class="govuk-error-summary__title">{{ t('common.errorSummaryTitle') }}</h2>
          <div class="govuk-error-summary__body">
            <ul class="govuk-list govuk-error-summary__list">
              <li><a href="#country">{{ error }}</a></li>
            </ul>
          </div>
        </div>
      {% endif %}

      <form method="post">
        <input type="hidden" name="crumb" value="{{ crumb }}">

        <div class="govuk-form-group{% if error %} govuk-form-group--error{% endif %}">
          <h1 class="govuk-label-wrapper">
            <label class="govuk-label govuk-label--l" for="country" data-testid="page-heading">
              {{ heading }}
            </label>
          </h1>
          {% if hint %}
            <div class="govuk-hint">{{ hint }}</div>
          {% endif %}
          {% if error %}
            <p class="govuk-error-message" id="country-error">
              <span class="govuk-visually-hidden">Error:</span> {{ error }}
            </p>
          {% endif %}
          <select class="govuk-select{% if error %} govuk-select--error{% endif %}"
                  id="country"
                  name="country"
                  {% if error %}aria-describedby="country-error"{% endif %}
                  data-testid="country-input">
            <option value=""></option>
            {% for c in countries %}
              <option value="{{ c }}"{% if c == country %} selected{% endif %}>{{ c }}</option>
            {% endfor %}
          </select>
        </div>

        <button type="submit" class="govuk-button" data-module="govuk-button"
                data-testid="continue-button">
          {{ continueButton }}
        </button>
      </form>

      <a href="{{ cancelUrl }}" class="govuk-link" data-testid="cancel-link">{{ cancelLink }}</a>

    </div>
  </div>
{% endblock %}
